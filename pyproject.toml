[project]
name = "comfui-blog"
version = "0.1.0"
description = "CDK code for creating a SageMaker processing job running ComfyUI workflows"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "aws-cdk-lib>=2.212.0",
    "cdk-nag>=2.37.10",
    "dotenv>=0.9.9",
    "pydantic>=2.11.7",
    "python-dotenv>=1.1.1",
    "pyyaml>=6.0.2",
]
